---
id: TASK-0009.01
title: agent-lib.sh - core CLI subcommands
status: In Progress
assignee: []
created_date: '2026-04-19 20:08'
updated_date: '2026-04-20 00:06'
labels:
  - cli
  - install
dependencies: []
parent_task_id: TASK-9
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
agent-lib.sh bash script with subcommands: install <slug> [--harness] [--version] [--server], search <query> [--type] [--harness], list [--type] [--harness], pull <slug> (download raw file), push <file> (upload artifact), info <slug>. Reads AGENT_LIB_SERVER env var. install accepts agent-group slugs and installs all member agents in role order (manager, analyse, implementation) in a single operation, showing progress per member.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 install downloads artifact and places it at correct harness path
- [ ] #2 search prints matching artifact slugs and descriptions
- [ ] #3 list prints all artifacts with type and harness columns
- [ ] #4 push uploads a local artifact file to the server
- [ ] #5 All subcommands show usage when called with --help
- [ ] #6 install with an agent-group slug installs all member agents in one command
- [ ] #7 install output labels each member with its role as it is placed
- [ ] #8 install --dry-run for a group lists all members that would be installed without writing files
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Create scripts/agent-lib.sh — Main CLI entrypoint (Bash 3.2+)
   - Shebang: #!/usr/bin/env bash; set -euo pipefail
   - Source agent-lib-common.sh from same directory (dirname $0)
   - Parse AGENT_LIB_SERVER from env (required; error if unset)
   - Parse first positional arg as subcommand; dispatch to function
   - Subcommands: install, search, list, pull, push, info
   - Each subcommand function handles its own --help flag
   - Global --help prints full usage summary

2. Create scripts/agent-lib-common.sh — Shared helper functions
   - _require_cmd(): assert curl and jq are available, fatal if missing
   - _api_get(): curl -sSf -u "$AGENT_LIB_USER:$AGENT_LIB_PASS" "$AGENT_LIB_SERVER/api/v1/$1"
   - _api_post(): same with -X POST -d @- or -d "$body"
   - _api_post_file(): curl -sSf -u ... -F "file=@$1" for push
   - _die(): echo error to stderr, exit 1
   - _info(): echo to stderr (progress messages)
   - _resolve_harness(): detect harness from env or default to "claude"
   - Auth: read AGENT_LIB_USER + AGENT_LIB_PASS from env (or ~/.agent-lib/config)

3. Implement "search" subcommand
   - Usage: agent-lib search <query> [--type T] [--harness H]
   - Calls GET /api/v1/artifacts?q=<query>&type=T&harness=H
   - Formats response as columnar table: SLUG | TYPE | DESCRIPTION (truncated)
   - Uses jq to parse JSON array; prints each artifact on one line
   - Maps to AC #2

4. Implement "list" subcommand
   - Usage: agent-lib list [--type T] [--harness H]
   - Calls GET /api/v1/artifacts (with optional ?type=&harness= params)
   - Formats response as table: SLUG | TYPE | HARNESSES
   - Pagination: loops fetching page=0,1,... until results < size
   - Maps to AC #3

5. Implement "info" subcommand
   - Usage: agent-lib info <slug>
   - Calls GET /api/v1/artifacts/<slug>/latest
   - Pretty-prints all metadata fields: name, type, version, description, harnesses, tags, author
   - Shows install target path for each supported harness

6. Implement "install" subcommand — single artifact
   - Usage: agent-lib install <slug> [--harness H] [--version V] [--server URL] [--dry-run] [--fetch-only] [--dest DIR]
   - Step A: GET /api/v1/install/manifest?slugs=<slug>&harness=<H>
   - Step B: GET /api/v1/install/bundle?slugs=<slug> (downloads zip to temp)
   - Step C: For each entry in manifest.entries:
     - Resolve targetPath (expand ~ to $HOME)
     - If targetPath contains "#" (JSON merge), handle merge logic
     - Otherwise mkdir -p parent; extract file from zip to targetPath
   - Step D: Print summary per file placed
   - --fetch-only: skip placement; extract zip to --dest (default ./artifact/)
   - --dry-run: print planned actions, do not write
   - Maps to AC #1

7. Implement "install" subcommand — agent-group support
   - When slug resolves to agent-group type (detected from manifest response having multiple entries), the manifest already returns expanded members from server
   - For each member entry: print "[role] Installing <slug> -> <targetPath>"
   - Progress labels each member with its role (from manifest metadata)
   - --dry-run lists all members that would be installed without writing
   - Maps to AC #6, #7, #8

8. Implement "push" subcommand
   - Usage: agent-lib push <file>
   - Reads local file; sends as POST /api/v1/artifacts with Content-Type: text/plain
   - File must contain valid YAML frontmatter
   - Prints confirmation: "Uploaded <slug> v<version>"
   - Maps to AC #4

9. Implement "pull" subcommand
   - Usage: agent-lib pull <slug> [--version V]
   - Calls GET /api/v1/artifacts/<slug>/<version> (or latest)
   - Writes raw content (frontmatter + body) to ./<primaryFileName>
   - Prints filename written

10. Implement --help for all subcommands
    - Each function checks $1 == "--help" || $1 == "-h"
    - Prints usage block with examples
    - Global help: prints all subcommands with one-line description
    - Maps to AC #5

11. Add install manifest role field to API response (if not present)
    - Check InstallEntry record — currently has slug, type, sourcePath, targetPath
    - For group installs, CLI needs to know each entry's role for labelling\n    - Option A: add optional "role" field to InstallEntry (null for non-group)\n    - Option B: CLI calls /artifacts/<slug>/members separately for group slugs\n    - Prefer Option A: extend InstallEntry with nullable role field\n    - Update InstallManifestResolver to populate role from ResolvedGroupMember\n\n12. Testing strategy\n    - Manual testing with running server instance\n    - Script self-test mode: agent-lib --self-test (calls each subcommand against server)\n    - ShellCheck validation: all scripts pass shellcheck without warnings\n    - Future: bats-core tests against ephemeral server container (not in this task scope)\n    - Ensure scripts work with Bash 3.2 (macOS default) — no associative arrays, no |& etc.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Self-review complete. AC coverage verified:
- AC #1 (install downloads & places): Steps 6-7 cover manifest fetch, bundle download, file placement
- AC #2 (search prints slugs+descriptions): Step 3 covers search formatting
- AC #3 (list prints type+harness columns): Step 4 covers columnar list output
- AC #4 (push uploads): Step 8 covers POST with text/plain body
- AC #5 (--help on all subcommands): Step 10 covers help handling
- AC #6 (group installs all members): Step 7 covers group expansion via manifest
- AC #7 (labels each member with role): Step 7 specifies "[role] Installing..." output format
- AC #8 (--dry-run for groups): Step 7 specifies dry-run lists without writing

Dependency: Step 11 adds "role" field to InstallEntry record (Java change) — this is a minor server-side addition needed for AC #7. Without it, CLI cannot label roles. This is a cross-task dependency with the existing M8 install API code.

No unverified assumptions. curl, jq, Bash 3.2+ are stated runtime deps in design doc. API endpoints confirmed from ArtifactApiController and InstallApiController code review.

Error handling covered: _die() for fatal errors, HTTP error codes from curl -f, missing env var checks, missing argument validation per subcommand.

Analysis complete. Plan ready. No blockers.

🔍 PLAN REVIEW CONCERNS:

1. **Step 11 scope ambiguity — Java change in a Bash task:** The plan proposes adding a "role" field to InstallEntry (a Java record). This is a server-side code change, but the task is described as "agent-lib.sh bash script." Either: (a) commit to Option B — call GET /api/v1/artifacts/{slug}/members from the CLI to get roles (no server change needed, endpoint already exists), or (b) explicitly state that Step 11 is a prerequisite server change in scope for this task. The plan cannot leave both options open.

2. **Zip extraction tool not specified (Step 6-C):** Plan says "extract file from zip to targetPath" but never names the extraction tool. macOS has /usr/bin/unzip, but Docker Alpine may not. Is unzip a runtime dep alongside curl/jq? Or does the script use python3 -m zipfile? Must be explicit — Implementation cannot guess.

3. **JSON merge logic unspecified (Step 6-C):** "If targetPath contains # (JSON merge), handle merge logic" is too vague. Merging into settings.json with jq is non-trivial (read file, inject at key path, write back, handle file-not-exists). This needs 3-4 sub-steps or a reference to a specific jq pattern.

4. **~/.agent-lib/config fallback mentioned but never implemented:** Step 2 references reading credentials from ~/.agent-lib/config as a fallback, but no plan step implements config file parsing. Either add a step for it or remove the mention and defer to a future task.

5. **Testing strategy has zero automated tests:** Plan defers bats-core to "future" and relies on manual testing + ShellCheck. ShellCheck validates syntax only. At minimum, include 2-3 inline self-test assertions (e.g. agent-lib --self-test that verifies curl/jq available, parses a mock response, confirms output format) or accept that AC verification is manual-only and state that explicitly.

Verdict: Plan needs 5 clarifications before implementation can proceed without guessing.
<!-- SECTION:NOTES:END -->
