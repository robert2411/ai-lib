---
id: TASK-0007.05
title: New and Edit form with delete
status: In Progress
assignee: []
created_date: '2026-04-19 20:08'
updated_date: '2026-04-19 23:15'
labels:
  - ui
dependencies: []
parent_task_id: TASK-7
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
EditController GET /new (empty form), GET /edit/{slug}/{version} (pre-filled), POST /save (create/update), POST /delete/{slug}/{version}. new.html and edit.html with Monaco editor for content. Requires ROLE_EDITOR.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 New form saves new artifact and redirects to detail page
- [ ] #2 Edit form pre-fills existing content and metadata
- [ ] #3 Delete button removes artifact and redirects to browse
- [ ] #4 Validation errors shown inline
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. **URL Rationale (deviation from design doc):** Design doc specifies `/edit/{type}/{name}` but this plan uses `/edit/{slug}/{version}`. Rationale: Including {version} in the edit URL allows editing a specific version (not just latest), which is required for the pre-fill logic. The {type} segment is omitted because ArtifactService resolves by slug alone. This is simpler and consistent with TASK-0007.04 URL pattern (`/artifacts/{slug}/{version}`).
2. **CSRF Note:** CSRF is disabled globally in SecurityConfig (csrf.disable()). This is acceptable for a team-internal application behind a TLS proxy with authentication. No th:action CSRF token is needed in form templates. No changes required.
3. Create src/main/java/com/agentlibrary/web/EditController.java in package com.agentlibrary.web:
   - @Controller, @PreAuthorize("hasRole("EDITOR")") at class level
   - Constructor-injected with ArtifactService
   - @GetMapping("/new") → returns "edit" view with empty form model
   - @GetMapping("/edit/{slug}/{version}") → returns "edit" view with pre-filled form model
   - @PostMapping("/save") → processes form submission, redirects to artifact detail
   - @PostMapping("/delete/{slug}/{version}") → deletes artifact, redirects to /browse
4. Create src/main/java/com/agentlibrary/web/ArtifactForm.java — form-backing object:
   - Fields: name (String), title (String), type (String), version (String), description (String), harnesses (List<String>), tags (String — comma-separated), category (String), language (String), author (String), visibility (String), content (String — the body content edited in Monaco)
   - Jakarta Validation annotations: @NotBlank on name, type, version; @Pattern on name for slug format
   - **NO toRawContent() method** — instead, the controller will construct an Artifact record from form fields and call MetadataCodec.encode(Artifact) to serialize back to raw content (frontmatter + body). This avoids duplicating serialization logic.
5. Implement GET /new handler:
   - Create empty ArtifactForm with sensible defaults (version="1.0.0", visibility="team")
   - Add "form" to model, "isNew" = true, "pageTitle" = "New Artifact"
   - Add ArtifactType.values() as "types" and Harness.values() as "harnesses" for dropdowns
   - Return "edit"
6. Implement GET /edit/{slug}/{version} handler:
   - Fetch artifact: artifactService.get(slug, version)
   - Populate ArtifactForm from artifact metadata and content
   - Tags: join list with ", " for display in text field
   - Harnesses: set as selected checkboxes
   - Add "form" to model, "isNew" = false, "pageTitle" = "Edit: " + title
   - Return "edit"
7. Implement POST /save handler:
   - Accept @Valid @ModelAttribute ArtifactForm, BindingResult, RedirectAttributes, Principal
   - If bindingResult.hasErrors(): return "edit" with form and errors for inline display
   - **Convert form to raw content:** Construct an Artifact record from form fields (build Metadata from name, title, type, version, description, harnesses, tags, category, language, author, visibility; set content from form.content). Then call MetadataCodec.encode(artifact) to produce the raw string with YAML frontmatter + body.
   - Extract User from Principal (reuse pattern from ArtifactApiController.extractUser)
   - Call artifactService.createOrUpdate(rawContent, user)
   - Catch ValidationException: add field errors to model, return "edit"
   - On success: add flash attribute "success" = "Artifact saved successfully"
   - Redirect to /artifacts/{slug}/{version}
8. Implement POST /delete/{slug}/{version} handler:
   - @PreAuthorize("hasRole("EDITOR")") (inherited from class)
   - Extract User from Principal
   - Call artifactService.delete(slug, user)
   - Add flash "success" = "Artifact deleted"
   - Redirect to /browse
9. Create src/main/resources/templates/edit.html using layout.html decorator:
   - Form with th:object="${form}" th:action="@{/save}" method="post"
   - Metadata fields: name (text input, readonly if !isNew), title, type (select from types), version (text input)
   - Description textarea
   - Harnesses: checkboxes for each Harness value
   - Tags: text input (comma-separated)
   - Category, language, author, visibility (select: team/private) inputs
   - Content area: hidden textarea (th:field="*{content}") synced with Monaco editor
   - Monaco editor div: initialised in page-specific scripts block using Monaco CDN loader
   - Validation errors: th:errors="*{fieldName}" displayed as inline red text below each field
   - Buttons row: "Save" submit button, "Delete" button (only if !isNew, styled as danger, confirms via JS dialog)
   - Delete form: separate <form th:action="@{/delete/{slug}/{version}(slug=${form.name}, version=${form.version})}" method="post"> with confirmation
   - Note: No CSRF hidden field needed — CSRF is disabled globally
10. Monaco editor integration (in layout:fragment="scripts"):
    - Load Monaco from CDN: require.config paths to CDN, require monaco editor
    - Initialise editor on div#monaco-editor with language "markdown" (or detect from type)
    - Sync editor content to hidden textarea on form submit (addEventListener on form submit event)
    - Set editor value from hidden textarea content on page load (for edit mode)
11. Helper method in EditController — extractUser(Principal):
    - Same pattern as API controller: check Authentication, extract roles, return User record
12. Test approach — create EditControllerTest.java:
    - @WebMvcTest(EditController.class) with @MockBean ArtifactService, Bucket
    - Test GET /new with @WithMockUser(roles="EDITOR") returns 200, view "edit", model has empty form and isNew=true
    - Test GET /new without EDITOR role returns 403
    - **Test GET /new with @WithMockUser(roles="ADMIN") returns 200** — verify ADMIN can access edit endpoints (role hierarchy)
    - Test GET /edit/{slug}/{version} with @WithMockUser(roles="EDITOR") returns 200, form pre-filled
    - **Test GET /edit/{slug}/{version} with @WithMockUser(roles="ADMIN") returns 200** — positive ADMIN access test
    - Test POST /save with valid data: verify artifactService.createOrUpdate called with output of MetadataCodec.encode(), redirect to detail
    - Test POST /save with invalid data (blank name): returns 200 (form page) with errors
    - Test POST /save with ValidationException: returns form with error message
    - Test POST /delete/{slug}/{version}: verify artifactService.delete called, redirect to /browse
    - Test all endpoints reject @WithMockUser(roles="USER") (403)
13. AC mapping:
    - AC#1 (new form saves and redirects): POST /save → construct Artifact from form → MetadataCodec.encode() → createOrUpdate → redirect /artifacts/{slug}/{version}
    - AC#2 (edit form pre-fills): GET /edit populates ArtifactForm from existing artifact
    - AC#3 (delete button removes and redirects): POST /delete → delete → redirect /browse
    - AC#4 (validation errors inline): BindingResult + th:errors inline display
14. Dependencies: Requires TASK-0007.01 (layout.html, Monaco setup). Independent of TASK-0007.02/03/04.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Analysis complete. Plan ready. No blockers. Note: Implementation agent should check if MetadataCodec has an encode() method; if not, create one or build frontmatter manually in ArtifactForm.toRawContent().

Self-review complete. All 4 AC mapped. Confirmed: MetadataCodec.decode() exists for parsing, can reverse-engineer encode logic from existing decode. ArtifactService.createOrUpdate() accepts raw content string with frontmatter. ValidationException is caught and can be mapped to form errors. @PreAuthorize at class level covers all endpoints. Potential issue: MetadataCodec may not have a public encode() method — implementation will need to construct frontmatter YAML manually (using SnakeYAML DumperOptions) or add an encode method. This is an implementation detail, not a blocker. Monaco CDN loading confirmed in plan (no local bundle needed).

⚠️ PLAN REVIEW CONCERNS:

- Concern #1: URL pattern mismatch with design doc. Design doc specifies /edit/{type}/{name} but plan uses /edit/{slug}/{version}. The {version} in the edit URL is unusual — typically you edit the latest version. Also missing {type} from URL. Should align with design doc or document deviation.

- Concern #2: MetadataCodec.encode() existence confirmed — plan notes say "may not have public encode()" but it DOES exist (line 59 of MetadataCodec.java: public static String encode(Artifact artifact)). Plan should state clearly: "Use MetadataCodec.encode() to convert form back to raw content" instead of suggesting manual YAML building.

- Concern #3: ArtifactForm.toRawContent() duplication risk. Since MetadataCodec.encode(Artifact) already exists, the form should construct an Artifact record and call MetadataCodec.encode() rather than reimplementing YAML serialization in the form class. Plan step 2 should clarify this.

- Concern #4: CSRF is globally disabled (csrf.disable() in SecurityConfig), so form POSTs will work. However, the plan does not explicitly state whether CSRF should be re-enabled for form endpoints. Since CSRF is disabled globally and the task is to add forms, this is a security consideration worth documenting even if the decision is "keep it disabled" (acceptable for team-internal app behind auth + TLS proxy).

- Concern #5: Role hierarchy means @PreAuthorize("hasRole(EDITOR)") will also admit ADMIN users (correct). But the test plan says "Test all endpoints reject USER role (403)" — good. Also verify ADMIN users CAN access (positive test).

Verdict: Plan needs revision — clarify MetadataCodec.encode() usage, fix URL pattern documentation, note CSRF stance explicitly.

Plan revised per Plan Reviewer feedback (5 concerns addressed):
1. Added explicit URL pattern rationale (step 1): /edit/{slug}/{version} is intentional, consistent with detail page pattern.
2. Updated to clearly state MetadataCodec.encode(Artifact) exists and must be used (steps 4, 7, 13).
3. Removed ArtifactForm.toRawContent() — controller constructs Artifact record from form fields then calls MetadataCodec.encode() (step 4).
4. Added CSRF stance note (step 2): disabled globally, team-internal app behind TLS proxy, no token needed.
5. Added @WithMockUser(roles="ADMIN") positive access tests (step 12): verify ADMIN can reach edit endpoints via role hierarchy.

Self-review complete. Plan covers all AC. No gaps or unverified assumptions.

✅ PLAN APPROVED — plan is complete, all AC covered, no ambiguity
- Steps verified: 14
- AC mapped: 4/4
- Concern #1 (URL pattern): Addressed in step 1 with explicit rationale
- Concern #2 (MetadataCodec.encode): Clearly referenced in steps 4, 7, 13
- Concern #3 (no toRawContent duplication): Explicitly removed in step 4
- Concern #4 (CSRF stance): Documented in step 2 — disabled globally, acceptable
- Concern #5 (ADMIN role test): Added in step 12 with @WithMockUser(roles="ADMIN")
<!-- SECTION:NOTES:END -->
