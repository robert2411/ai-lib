---
id: TASK-0003.02
title: IndexFile - read/write INDEX.yaml
status: In Progress
assignee: []
created_date: '2026-04-19 20:05'
updated_date: '2026-04-19 21:31'
labels:
  - storage
dependencies: []
parent_task_id: TASK-3
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
IndexFile reads and writes INDEX.yaml in the git repo root. Maintains a list of artifact entries (slug, type, harness, latestVersion, versions[]). Atomic update: read-modify-write under WriteQueue.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 IndexFile.load parses existing INDEX.yaml
- [x] #2 IndexFile.save writes a valid YAML file readable by load
- [x] #3 Missing INDEX.yaml results in empty index, not exception
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
## Implementation Plan — TASK-0003.02: IndexFile - read/write INDEX.yaml

### Package
`com.agentlibrary.storage`

### Files to Create

#### 1. `src/main/java/com/agentlibrary/storage/IndexFile.java`

Utility class that reads/writes the INDEX.yaml file. INDEX.yaml is a flat list of artifact metadata entries maintained inside the git working tree (or bare repo object tree).

```java
package com.agentlibrary.storage;

import com.agentlibrary.model.ArtifactMetadata;
import java.util.List;

public class IndexFile {
    private IndexFile() {}

    public static List<ArtifactMetadata> load(String yamlContent);
    public static String save(List<ArtifactMetadata> entries);
}
```

**Design decisions:**
- `load(String yamlContent)` takes raw YAML string (not a file path). This keeps IndexFile decoupled from git — the caller (LocalGitArtifactRepository) reads the blob content from git and passes it in.
- `save(List<ArtifactMetadata>)` returns a YAML string. Caller writes it to git.
- Null or empty input to `load` returns `Collections.emptyList()` — satisfies AC#3.

**YAML structure of INDEX.yaml:**

```yaml
artifacts:
  - name: git-helper
    type: skill
    version: 1.2.0
    title: Git Helper
    description: One-line summary
    harnesses: [claude, copilot]
    tags: [git, workflow]
    category: developer-tools
    author: robert
    visibility: team
    created: 2026-04-19T10:00:00Z
    updated: 2026-04-19T10:00:00Z
  - name: other-artifact
    ...
```

**Implementation steps:**

1. **`load(String yamlContent)`:**
   - If `yamlContent` is null or blank, return `List.of()` (AC#3)
   - Parse YAML using SnakeYAML `new Yaml().load(yamlContent)` → `Map<String, Object>`
   - Extract `artifacts` key → `List<Map<String, Object>>`
   - For each map entry, convert to `ArtifactMetadata` using same field-mapping logic as MetadataCodec.mapToMetadata (extract into shared utility or duplicate minimal mapping)
   - Return unmodifiable list
   - If `artifacts` key missing or not a list, return `List.of()`

2. **`save(List<ArtifactMetadata> entries)`:**
   - Create `Map<String, Object>` with key `"artifacts"` → list of maps
   - For each ArtifactMetadata, convert to Map (slug-based enum values, ISO-8601 timestamps)
   - Serialize with SnakeYAML DumperOptions: BLOCK flow style, explicit start (---)
   - Return the YAML string

3. **Shared mapping concern:**
   - MetadataCodec already has `mapToMetadata` and `metadataToMap` (private). 
   - **Approach:** Extract a package-private `MetadataMapper` helper in `com.agentlibrary.metadata` with static methods `toMap(ArtifactMetadata)` and `fromMap(Map<String,Object>)`. 
   - Alternatively, make MetadataCodec methods package-private or add public static utility methods.
   - **Recommended:** Add two public static methods to MetadataCodec:
     - `MetadataCodec.metadataFromMap(Map<String,Object>): ArtifactMetadata`
     - `MetadataCodec.metadataToMap(ArtifactMetadata): Map<String,Object>`
   - This avoids code duplication between IndexFile and MetadataCodec.
   - The Implementation agent should refactor MetadataCodec to expose these two methods as public static.

### Test Approach

#### `src/test/java/com/agentlibrary/storage/IndexFileTest.java`

Test cases:
1. **load with null returns empty list** (AC#3)
2. **load with empty string returns empty list** (AC#3)
3. **load with valid YAML returns correct metadata list** (AC#1)
   - Create a YAML string with 2-3 entries, verify parsed list matches
4. **load with missing artifacts key returns empty list**
5. **save produces valid YAML readable by load** (AC#2) — round-trip test
   - Create list of ArtifactMetadata, save → string → load, verify equality
6. **save with empty list produces valid YAML with empty artifacts list**
7. **Ordering is preserved** — save [A, B, C], load, verify same order
8. **All metadata fields survive round-trip** — test entry with all optional fields populated

### Dependencies
- Milestone 1 domain model (ArtifactMetadata, ArtifactType, Harness, Visibility, etc.)
- Milestone 2 MetadataCodec (for refactored shared mapping methods)
- SnakeYAML (already in pom.xml)
- No dependency on other Milestone 3 tasks
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Self-review complete. Plan covers all AC: AC#1 (load parses INDEX.yaml) → step 1 + tests 3,4; AC#2 (save writes valid YAML) → step 2 + test 5 round-trip; AC#3 (missing file = empty index) → step 1 null handling + tests 1,2. No gaps. Note: requires minor refactor of MetadataCodec to expose mapping methods publicly — not a blocker, just an implementation detail.

Analysis complete. Plan ready. No blockers.

✅ PLAN APPROVED — plan is complete, all AC covered, no ambiguity
- Steps verified: 3 (load, save, MetadataCodec refactor)
- AC mapped: 3/3 (AC#1→load+test3, AC#2→save+roundtrip test5, AC#3→null handling+tests1-2)
- YAML structure aligned with design doc INDEX.yaml spec
- Decoupled from git (takes String, returns String) — clean interface
- Shared mapping refactor of MetadataCodec is additive and non-breaking

Implementation complete. All AC verified.
- IndexFile.load handles null/empty/valid YAML
- IndexFile.save produces round-trip safe YAML
- MetadataCodec refactored with public metadataFromMap/metadataToMap
- Fixed SnakeYAML Date auto-parse in parseInstant
- 9 unit tests all passing

✅ QA APPROVED — all tests passing, no regressions
- AC/DoD: AC #1-3 satisfied
- IndexFile load/save round-trip validated
- Mapping via MetadataCodec public methods is clean and covered by tests
<!-- SECTION:NOTES:END -->
