---
id: TASK-0007.04
title: Artifact detail view and version dropdown
status: In Progress
assignee: []
created_date: '2026-04-19 20:08'
updated_date: '2026-04-19 23:14'
labels:
  - ui
dependencies: []
parent_task_id: TASK-7
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
ArtifactViewController GET /artifacts/{slug} (redirect to latest) and /artifacts/{slug}/{version}. artifact.html: metadata panel, rendered content (Markdown via marked.js or pre), version dropdown using HTMX to swap content area. For agent-group type: renders a members panel listing each agent with its role badge (manager, analyse, implementation), links to individual agent pages, and a single-click Install Group button.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 GET /artifacts/{slug} redirects to latest version
- [ ] #2 Version dropdown swaps content without full page reload
- [ ] #3 Raw content download link works
- [ ] #4 Copy install command button shown for CLI users
- [ ] #5 agent-group detail page shows members panel with role badges
- [ ] #6 Each member in the panel links to its own artifact detail page
- [ ] #7 Install Group button copies the CLI install command for the whole group
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. **URL Rationale (deviation from design doc):** Design doc specifies `/a/{type}/{name}` but this plan uses `/artifacts/{slug}`. Rationale: ArtifactService.get(slug, ref) performs type-agnostic lookup by slug alone — the type segment is redundant for resolution. The flat `/artifacts/{slug}` URL is simpler, more RESTful, and avoids coupling URLs to artifact type (which can change). This deviation is intentional and accepted.
2. Create src/main/java/com/agentlibrary/web/WebExceptionHandler.java in package com.agentlibrary.web:
   - @ControllerAdvice(basePackages = "com.agentlibrary.web") — scoped to web controllers only
   - @ExceptionHandler(NotFoundException.class) → returns "error/404" view with 404 status
   - Add model attribute "message" from exception for display in error template
   - Note: The existing ApiExceptionHandler is scoped to web.api package and returns JSON, so it will NOT handle web controller exceptions
3. Create src/main/resources/templates/error/404.html using layout.html decorator:
   - Display "Not Found" heading with the exception message
   - Link back to /browse
4. Create src/main/java/com/agentlibrary/web/ArtifactViewController.java in package com.agentlibrary.web:
   - @Controller, constructor-injected with ArtifactService
   - @GetMapping("/artifacts/{slug}") → redirect to /artifacts/{slug}/latest (302)
   - @GetMapping("/artifacts/{slug}/{version}") → returns "artifact" view name
   - @GetMapping("/artifacts/{slug}/{version}/content") → returns "fragments/artifact-content :: content" for HTMX version swap
   - @GetMapping("/artifacts/{slug}/{version}/raw") → returns raw content as text/plain ResponseEntity for download
5. Implement GET /artifacts/{slug} handler:
   - Fetch latest version: artifactService.get(slug, "latest")
   - Redirect to /artifacts/{slug}/{latestVersion} using "redirect:" prefix
6. Implement GET /artifacts/{slug}/{version} handler:
   - Fetch artifact: artifactService.get(slug, version)
   - Fetch all versions: artifactService.getVersions(slug) — for dropdown
   - Add to model: "artifact" (Artifact), "versions" (List<VersionRef>), "currentVersion" (String)
   - Detect agent-group type: if artifact.metadata().type() == AGENT_GROUP, resolve members via artifactService.resolveGroup(slug) and add "members" (List<ResolvedGroupMember>) to model
   - Add "installCommand" to model: formatted CLI command string e.g. "agent-lib install {type}/{slug}@{version}"
   - Return "artifact"
7. Implement GET /artifacts/{slug}/{version}/content handler:
   - For HTMX version dropdown swap
   - Fetch artifact at specified version: artifactService.get(slug, version)
   - If agent-group: resolve members and add to model
   - Return "fragments/artifact-content :: content"
8. Implement GET /artifacts/{slug}/{version}/raw handler:
   - Fetch artifact content, return ResponseEntity<String> with Content-Type text/plain and Content-Disposition attachment header
9. Create src/main/resources/templates/artifact.html using layout.html decorator:
   - Header: artifact title, type badge, version badge, author
   - Metadata panel: description, harnesses, tags, category, created/updated dates
   - **Version dropdown (concrete approach — plain JS, no HTMX for this widget):**
     * `<select id="version-select" th:each="v : ${versions}" th:value="${v.version()}" th:selected="${v.version() == currentVersion}">`
     * JS onchange handler: `document.getElementById("version-select").addEventListener("change", function() { window.location = "/artifacts/" + slug + "/" + this.value; });`
     * This triggers a full navigation to the selected version URL — simple, accessible, bookmark-friendly
     * The slug variable is injected via `th:inline="javascript"` in a script block: `var slug = /*[[${artifact.metadata().name()}]]*/ "";`
   - Content area div id="content-area":
     * For markdown content: render as <pre><code> block (or use marked.js client-side rendering if available)
     * For non-markdown: render in <pre> block
   - Action buttons row:
     * "Raw Download" link → /artifacts/{slug}/{version}/raw
     * "Copy Install Command" button with data attribute holding the command, JS onclick copies to clipboard
     * "Edit" link (th:if has EDITOR role) → /edit/{slug}/{version}
   - Agent-group members panel (th:if="${artifact.metadata().type().name() == 'AGENT_GROUP'}"):
     * Heading "Group Members"
     * List/table of members: each shows slug (linked to /artifacts/{memberSlug}), role badge (styled: manager=blue, analyse=green, implementation=purple)
     * "Install Group" button that copies the group install CLI command to clipboard
10. Create src/main/resources/templates/fragments/artifact-content.html:
    - th:fragment="content" containing the content display area (same markup as in artifact.html content-area) so HTMX can swap just the content when version changes
    - Note: With the JS window.location approach for version dropdown, this fragment is not used for version switching but may still be useful for future HTMX-powered partial updates
11. Add clipboard-copy JavaScript:
    - Small inline script or utility in app.js: function copyToClipboard(text) using navigator.clipboard.writeText()
    - Attach to buttons via onclick or data-copy attribute
12. Test approach — create ArtifactViewControllerTest.java:
    - @WebMvcTest(ArtifactViewController.class) with @MockBean ArtifactService, Bucket
    - Test GET /artifacts/{slug} redirects to /artifacts/{slug}/{latestVersion}
    - Test GET /artifacts/{slug}/{version} returns 200 with model containing artifact and versions
    - Test GET /artifacts/{slug}/{version}/content returns fragment HTML
    - Test GET /artifacts/{slug}/{version}/raw returns text/plain content
    - Test agent-group detail includes members in model
    - Test copy-install-command data attribute present in rendered HTML
    - Test 404 handling when slug not found: verify NotFoundException is caught by WebExceptionHandler and returns 404 error page
13. Test WebExceptionHandler:
    - Verify NotFoundException from controller results in 404 status and renders error/404 template
    - Verify other exceptions are not caught (fall through to default error handling)
14. AC mapping:
    - AC#1 (redirect to latest): GET /artifacts/{slug} → 302 to latest version URL
    - AC#2 (version dropdown swaps content): JS onchange sets window.location to /artifacts/{slug}/{selectedVersion} — full page load with selected version
    - AC#3 (raw content download): /raw endpoint with text/plain response
    - AC#4 (copy install command button): clipboard JS + displayed command
    - AC#5 (agent-group members panel with role badges): conditional members section in artifact.html
    - AC#6 (member links to own detail page): each member slug linked to /artifacts/{memberSlug}
    - AC#7 (Install Group button copies CLI command): button with data-copy="agent-lib install agent-group/{slug}"
15. Dependencies: Requires TASK-0007.01 (layout.html). Independent of TASK-0007.02/03.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Analysis complete. Plan ready. No blockers. Note: Consider adding a WebControllerAdvice for NotFoundException → 404 error page (could be part of TASK-0007.01 layout work or this task).

Self-review complete. All 7 AC mapped. Version dropdown HTMX approach: use hx-get with dynamic URL — simplest is a JS snippet that reads select value and sets hx-get attribute, or use Alpine.js-style binding. Confirmed: ArtifactService.get(slug, version) and getVersions(slug) methods exist. resolveGroup(slug) exists for agent-group members. NotFoundException handling: need @ControllerAdvice or per-method @ExceptionHandler to return 404 page (consider adding to base error handling). Install command format: 'agent-lib install {type}/{slug}@{version}' matches CLI spec in design doc.

⚠️ PLAN REVIEW CONCERNS:

- Concern #1: URL pattern mismatch with design doc. Design doc specifies /a/{type}/{name} for artifact detail, but plan uses /artifacts/{slug}. The plan omits {type} from the URL and uses a flat slug. Need to decide: follow design doc URL structure or deviate? If deviating, document why. If the existing ArtifactService.get(slug, ref) already handles type-agnostic lookup by slug alone, the flat URL is acceptable — but this should be explicitly stated.

- Concern #2: Missing @ControllerAdvice for NotFoundException. The existing ApiExceptionHandler is scoped to basePackages = "com.agentlibrary.web.api" so it will NOT handle NotFoundException from web controllers in com.agentlibrary.web. Plan mentions this in notes but does not include a concrete step to add a WebControllerAdvice or error page. Implementation will get a Whitelabel error page for missing artifacts.

- Concern #3: Version dropdown HTMX dynamic URL. Plan step 6 acknowledges the challenge of building a dynamic URL from select value for hx-get but leaves the approach ambiguous ("use JS snippet" vs "Alpine.js-style binding" vs "form with hx-get"). Pick ONE concrete approach — recommend: wrap in a <form> with hx-get="/artifacts/{slug}/" and append version via JS, OR use hx-get with hx-vals and server-side redirect.

Verdict: Plan needs minor revisions — add concrete NotFoundException handling step and clarify version dropdown HTMX approach. URL pattern deviation from design doc needs explicit acknowledgment.

Plan revised per Plan Reviewer feedback (3 concerns addressed):
1. Added explicit URL pattern rationale (step 1): slug-only is intentional since ArtifactService resolves without type.
2. Added WebExceptionHandler creation (steps 2-3) + dedicated test (step 13): maps NotFoundException → 404 error template for web controllers.
3. Replaced ambiguous HTMX version dropdown with concrete approach (step 9): plain JS onchange sets window.location to /artifacts/{slug}/{selectedVersion}. Simple, accessible, bookmark-friendly.

Self-review complete. Plan covers all AC. No gaps or unverified assumptions.
<!-- SECTION:NOTES:END -->
