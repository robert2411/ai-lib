---
id: TASK-0002.05
title: 'MetadataValidator - per-type schemas, semver enforcement, slug rules'
status: Done
assignee: []
created_date: '2026-04-19 20:05'
updated_date: '2026-04-19 21:04'
labels:
  - domain
  - metadata
dependencies: []
parent_task_id: TASK-2
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
MetadataValidator validates ArtifactMetadata against per-type YAML schemas loaded from TypeSchemaRegistry. Enforces slug format (lowercase, hyphens), valid SemVer version, required fields per type, harness allowed values per type. Includes schema/agent-group.yaml defining the members[] structure with required slug and role fields.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Validation passes for fixture artifact of each ArtifactType
- [x] #2 Missing required field returns descriptive ValidationException
- [x] #3 Invalid slug format is rejected
- [x] #4 Invalid semver is rejected
- [x] #5 agent-group type requires at least one member with a recognised role
- [x] #6 agent-group with duplicate member slugs is rejected
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Depends on: TASK-0002.01 (enums), TASK-0002.02 (ArtifactMetadata), TASK-0002.03 (SemVer), TASK-0002.04 (MetadataCodec)
2. Create class ValidationException in com.agentlibrary.metadata.ValidationException:
   - File: src/main/java/com/agentlibrary/metadata/ValidationException.java
   - Extends RuntimeException
   - Field: List<String> errors (unmodifiable)
   - Constructor: ValidationException(List<String> errors)
   - Method: public List<String> errors()
   - Override getMessage() to join all errors with "; "
3. Create class TypeSchemaRegistry in com.agentlibrary.metadata.TypeSchemaRegistry:
   - File: src/main/java/com/agentlibrary/metadata/TypeSchemaRegistry.java
   - Loads per-type schema definitions from classpath YAML files
   - Schema location: src/main/resources/schemas/<type-slug>.yaml (e.g. schemas/skill.yaml, schemas/agent-group.yaml)
   - Each schema YAML defines:
     - required: [list of required field names]
     - optional: [list of optional field names]
     - allowedHarnesses: [list of harness slugs] (or "all")
   - Method: public static TypeSchema forType(ArtifactType type)
   - Fallback: if no schema file exists for a type, use a default schema with minimal required fields (name, type, version)
4. Create record TypeSchema in com.agentlibrary.metadata.TypeSchema:
   - File: src/main/java/com/agentlibrary/metadata/TypeSchema.java
   - Fields: Set<String> requiredFields, Set<String> optionalFields, Set<Harness> allowedHarnesses
5. Create schema YAML files:
   - src/main/resources/schemas/skill.yaml: required: [name, type, version, description], allowedHarnesses: [claude, copilot]
   - src/main/resources/schemas/agent-claude.yaml: required: [name, type, version], allowedHarnesses: [claude]
   - src/main/resources/schemas/agent-copilot.yaml: required: [name, type, version], allowedHarnesses: [copilot]
   - src/main/resources/schemas/agent-group.yaml: required: [name, type, version, members], allowedHarnesses: all, members schema: each member requires slug and role
   - src/main/resources/schemas/mcp.yaml: required: [name, type, version], allowedHarnesses: all
   - src/main/resources/schemas/command.yaml: required: [name, type, version], allowedHarnesses: [claude]
   - src/main/resources/schemas/hook.yaml: required: [name, type, version], allowedHarnesses: [claude]
   - src/main/resources/schemas/claude-md.yaml: required: [name, type, version], allowedHarnesses: [claude]
   - src/main/resources/schemas/opencode.yaml: required: [name, type, version], allowedHarnesses: all
   - src/main/resources/schemas/prompt.yaml: required: [name, type, version], allowedHarnesses: all
6. Create class MetadataValidator in com.agentlibrary.metadata.MetadataValidator:
   - File: src/main/java/com/agentlibrary/metadata/MetadataValidator.java
   - Method: public static void validate(ArtifactMetadata metadata) throws ValidationException
   - Validation steps (collect ALL errors then throw once):
     a. Slug validation: metadata.name() must match ^[a-z][a-z0-9-]*[a-z0-9]$ (or single char ^[a-z]$)
        - Lowercase letters, digits, hyphens only
        - Must start with letter
        - Must not end with hyphen
        - Must not contain consecutive hyphens
     b. SemVer validation: SemVer.isValid(metadata.version()) must be true
     c. Required fields: load TypeSchema for metadata.type(), check all required fields are non-null and non-blank
     d. Harness validation: each harness in metadata.harnesses() must be in schema allowedHarnesses (unless "all")
     e. Agent-group specific:
        - If type == AGENT_GROUP, members list must not be empty (AC #5)
        - Each member must have non-blank slug and a recognised role
        - No duplicate member slugs (AC #6)
     f. Collect all validation errors into a list
     g. If errors non-empty, throw ValidationException(errors)
7. Create test fixtures:
   - src/test/resources/fixtures/ directory
   - One valid fixture YAML per ArtifactType (10 files)
   - e.g. fixtures/valid-skill.yaml, fixtures/valid-agent-group.yaml, etc.
8. Create test file MetadataValidatorTest:
   - File: src/test/java/com/agentlibrary/metadata/MetadataValidatorTest.java
   - Test: validFixtureForEachType (AC #1) — for each ArtifactType, construct valid ArtifactMetadata fixture, validate passes
   - Test: missingRequiredFieldReturnsDescriptiveError (AC #2) — metadata with null version, validate throws ValidationException with message containing "version"
   - Test: invalidSlugRejected (AC #3):
     - "My Skill" (uppercase, space) rejected
     - "-leading-hyphen" rejected
     - "trailing-" rejected
     - "double--hyphen" rejected
     - "" (empty) rejected
   - Test: validSlugAccepted:
     - "my-skill", "a", "skill-v2", "my-long-artifact-name" all pass
   - Test: invalidSemverRejected (AC #4):
     - "not-a-version" rejected
     - "1.0" rejected
   - Test: agentGroupRequiresMembers (AC #5):
     - type=AGENT_GROUP with empty members list throws ValidationException
   - Test: agentGroupRequiresRecognisedRole (AC #5):
     - member with role="unknown" — decide: warn or reject? Plan: reject with descriptive error
   - Test: agentGroupDuplicateSlugsRejected (AC #6):
     - two members with same slug throws ValidationException with message about duplicates
   - Test: harnessValidation:
     - agent-claude type with harnesses=[COPILOT] rejected
9. Create test file TypeSchemaRegistryTest:
   - File: src/test/java/com/agentlibrary/metadata/TypeSchemaRegistryTest.java
   - Test: schemaExistsForEachType — TypeSchemaRegistry.forType(type) returns non-null for all ArtifactType values
   - Test: skillSchemaRequiresDescription — schema for SKILL includes "description" in required fields
   - Test: agentGroupSchemaRequiresMembers — schema for AGENT_GROUP includes "members" in required
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Analysis complete. Plan ready. No blockers.

Self-review complete. Plan covers all 6 AC: (1) valid fixture per type with test, (2) missing required field returns descriptive error, (3) invalid slug rejected with 5 specific invalid cases + valid cases, (4) invalid semver rejected, (5) agent-group requires at least one member with recognised role, (6) duplicate member slugs rejected. Error collection pattern specified (collect all then throw once). Slug regex fully specified. Schema registry fallback defined. Note: code dependency on MetadataCodec is NOT required — validator operates on ArtifactMetadata objects directly. Dependency is logical ordering only (test fixtures may use codec for convenience). TypeSchemaRegistry uses SnakeYAML independently to load schema YAML files from classpath.

✅ PLAN APPROVED — plan is complete, all 6 AC covered, no ambiguity
- Steps verified: 9
- AC mapped: 6/6
- Slug regex fully specified: ^[a-z][a-z0-9-]*[a-z0-9]$ + no consecutive hyphens
- Schema registry with per-type YAML files is a clean extensible approach
- Fallback schema for missing types prevents breakage
- Error collection pattern (collect all then throw once) — good UX
- Agent-group validation: non-empty members, recognised roles, no duplicate slugs
- Dependencies correct (uses SemVer.isValid from TASK-0002.03, model from 01/02)
- Note: TASK-0002.04 dependency is logical ordering only (not code dependency) — acknowledged in plan

Implementation complete. All AC verified. MetadataValidator with slug rules, semver enforcement, per-type schema validation, agent-group member/role/duplicate checks. TypeSchemaRegistry with 10 schema YAML files. 16 tests passing.

❌ QA REJECTED: AC/DoD incomplete and schema loading issue.
- Acceptance Criteria checkboxes are still unchecked.
- [Low] TypeSchemaRegistry does not close schema InputStream (resource leak risk) in loadSchema (src/main/java/com/agentlibrary/metadata/TypeSchemaRegistry.java:39-45).
- Definition of Done checklist is not fully completed.

QA fix applied: TypeSchemaRegistry.loadSchema() now uses try-with-resources to properly close InputStream.

✅ QA APPROVED — all tests passing, no regressions
- AC: All checklist items marked complete
- Re-review verified TypeSchemaRegistry.loadSchema uses try-with-resources for InputStream handling (TypeSchemaRegistry.java).
- MetadataValidator/registry tests passing in full suite.

✅ SECURITY APPROVED — static audit complete, zero vulnerabilities identified
- Files reviewed: MetadataValidator.java, ValidationException.java
- Checks: OWASP Top 10, path traversal, ReDoS, input validation
- Slug regex ReDoS analysis: SLUG_PATTERN ^[a-z][a-z0-9-]*[a-z0-9]$ has overlap between [a-z0-9-]* and [a-z0-9] but only a single quantifier — worst case is O(n) linear backtracking, NOT exponential. CONSECUTIVE_HYPHENS "--" is a fixed literal, zero backtracking. SINGLE_CHAR_SLUG ^[a-z]$ is trivial. No ReDoS risk.
- Notes: Validator is pure read-only logic collecting errors into a list. ValidationException uses List.copyOf for immutability. No side effects, no I/O, no injection sinks. Clean.
<!-- SECTION:NOTES:END -->
