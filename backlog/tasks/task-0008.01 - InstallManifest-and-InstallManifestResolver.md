---
id: TASK-0008.01
title: InstallManifest and InstallManifestResolver
status: In Progress
assignee:
  - '@myself'
created_date: '2026-04-19 20:08'
updated_date: '2026-04-19 23:54'
labels:
  - install
dependencies: []
parent_task_id: TASK-8
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
InstallManifest record: list of InstallEntry (source path in repo, targetPath resolved per harness). InstallManifestResolver resolves target paths for each artifact type + harness combination using a harness path config map. For agent-group slugs: resolver expands the group into its member agents and returns one InstallEntry per member, preserving role order (manager, analyse, implementation).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Resolver returns correct target path for each ArtifactType x Harness combination
- [x] #2 Unknown harness throws descriptive exception
- [x] #3 Manifest serialises to JSON for API response
- [x] #4 Passing an agent-group slug expands to one entry per member agent
- [x] #5 Expanded group entries are ordered: manager, analyse, implementation, custom
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Create InstallEntry record in com.agentlibrary.install package:
   - Fields: String slug, ArtifactType type, String sourcePath (relative path within bundle archive, e.g. "skill.md"), String targetPath (resolved per harness)
   - Immutable Java record with null validation on all fields

2. Create InstallManifest record in com.agentlibrary.install package:
   - Fields: List<InstallEntry> entries, Harness harness
   - Record is serialisable to JSON via Jackson (Spring Boot auto-config handles this)
   - AC#3: The record structure IS the JSON shape — no separate DTO needed since records serialise naturally

3. Create InstallManifestResolver class in com.agentlibrary.install package (Spring @Component):
   - Constructor-injected with ArtifactService (for resolving group members and getting artifact metadata)
   - Define HARNESS_TARGET_PATHS static Map<Harness, Map<ArtifactType, String>> with path templates:
     * CLAUDE + SKILL → "~/.claude/skills/{name}/"
     * CLAUDE + AGENT_CLAUDE → ".claude/agents/{name}.md"
     * CLAUDE + MCP → ".claude/settings.json#mcpServers.{name}"
     * CLAUDE + COMMAND → "~/.claude/commands/{name}.md"
     * CLAUDE + HOOK → ".claude/settings.json#hooks.{event}"
     * CLAUDE + CLAUDE_MD → "CLAUDE.md"
     * CLAUDE + PROMPT → "~/.claude/prompts/{name}.md"
     * COPILOT + AGENT_COPILOT → ".github/agents/{name}.md"
     * COPILOT + MCP → ".vscode/mcp.json#servers.{name}"
     * (other combinations as appropriate — OPENCODE for copilot etc.)
   - Target path precedence: if artifact metadata has install.target set (InstallConfig.target()), use that template. Otherwise fall back to HARNESS_TARGET_PATHS. This matches design doc where metadata can override defaults.
   - Method: InstallManifest resolve(List<String> slugs, Harness harness)
     * For each slug, call ArtifactService.get(slug, "latest") to retrieve metadata
     * If artifact type is AGENT_GROUP: expand via ArtifactService.resolveGroup(slug) → one InstallEntry per member agent (AC#4). Each member gets its own resolved targetPath based on member type.
     * For non-group artifacts: resolve targetPath by checking metadata.install().target() first, then harness map fallback, substituting {name} with artifact name
     * sourcePath: derive from ArtifactType primary file convention (skill→"skill.md", agent-claude→"agent.md", agent-copilot→"agent.md", mcp→"mcp.json", command→"command.md", hook→"hook.json", claude-md→"snippet.md", opencode→"config.yaml", prompt→"prompt.md")
     * AC#2: If harness has no mapping for the given type AND metadata has no install config, throw IllegalArgumentException("No install path for <type> on harness <harness>")
     * AC#5: Group expansion preserves role order from ArtifactService.resolveGroup() which already sorts: manager=0, analyse=1, implementation=2, custom=3
   - Private method: String resolveTargetPath(ArtifactMetadata metadata, Harness harness)
     * Check metadata.install() != null && metadata.install().target() != null → use it
     * Otherwise look up HARNESS_TARGET_PATHS map
     * Substitute {name} placeholder with metadata.name()
     * Returns resolved path string
   - Private static method: String primaryFileName(ArtifactType type) — returns the canonical primary file name per type

4. Create InstallManifestResolverTest (unit test, Mockito):
   - Mock ArtifactService
   - Test: resolve skill for claude → correct ~/.claude/skills/{name}/ path (AC#1)
   - Test: resolve agent-claude for claude → .claude/agents/{name}.md (AC#1)
   - Test: resolve agent-copilot for copilot → .github/agents/{name}.md (AC#1)
   - Test: resolve MCP for claude → .claude/settings.json#mcpServers.{name} (AC#1)
   - Test: resolve skill with custom install.target → uses metadata target over default (AC#1)
   - Test: unknown harness slug passed to Harness.fromSlug() → IllegalArgumentException with descriptive message (AC#2)
   - Test: valid harness + unsupported type combo (e.g., OPENCODE for claude) with no install config → IllegalArgumentException (AC#2)
   - Test: resolve agent-group slug → expand to member entries, verify order manager→analyse→implementation→custom (AC#4, AC#5)
   - Test: resolve agent-group with mixed roles → correct ordering (AC#5)
   - Test: JSON serialisation of InstallManifest (AC#3) — serialise via ObjectMapper, verify JSON has "entries" array with slug/type/sourcePath/targetPath and "harness" field
   - Test: empty slugs list → returns manifest with empty entries list (no error)

5. File locations:
   - src/main/java/com/agentlibrary/install/InstallEntry.java
   - src/main/java/com/agentlibrary/install/InstallManifest.java
   - src/main/java/com/agentlibrary/install/InstallManifestResolver.java
   - src/test/java/com/agentlibrary/install/InstallManifestResolverTest.java

6. Dependencies:
   - Depends on: ArtifactService.get(), ArtifactService.resolveGroup(), Harness enum, ArtifactType enum, InstallConfig record (all exist)
   - No new Maven dependencies needed
   - TASK-0008.02 depends on this task
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Self-review complete. Plan covers all AC:
- AC#1: HARNESS_TARGET_PATHS map + metadata.install().target() precedence covers all type×harness combos
- AC#2: Two paths — Harness.fromSlug() for truly unknown harness + resolver error for unsupported type combo
- AC#3: Jackson auto-serialises records; explicit test verifies JSON shape
- AC#4: AGENT_GROUP expansion via existing ArtifactService.resolveGroup()
- AC#5: Role ordering preserved from resolveGroup() (manager=0, analyse=1, implementation=2, custom=3)

No unverified assumptions. No blockers.
Key design decisions:
- sourcePath = primary file name per type convention (e.g. "skill.md"), not full repo path
- metadata install.target takes precedence over hardcoded harness map
- Empty slugs list is valid (returns empty manifest)

✅ PLAN APPROVED — plan is complete, all AC covered, no ambiguity
- Steps verified: 6
- AC mapped: 5/5
- Verified: ArtifactService.get(slug, ref), resolveGroup(slug), Harness.fromSlug(), InstallConfig.target() all exist
- Verified: ResolvedGroupMember record exists with slug()/role()/metadata()
- Verified: resolveGroup() already sorts by role order (manager=0, analyse=1, implementation=2, custom=3)
- Target paths consistent with design doc CLI install section
- No unverified assumptions remain

- Implemented InstallEntry record with null validation
- Implemented InstallManifest record (Jackson-serialisable)
- Implemented InstallManifestResolver with HARNESS_TARGET_PATHS map + metadata override
- 18 unit tests passing covering all AC
- Coverage: all paths tested (happy path, error paths, edge cases)

All AC/DoD checked. Ready for QA.

✅ QA APPROVED — all tests passing, no regressions
- AC/DoD: Complete
- Code quality: Good
- Security: No issues found in resolver/manifest implementation
- Spelling: Clean
- Verification: Reviewed implementation + tests; full test suite passes (./mvnw test -q).
<!-- SECTION:NOTES:END -->
