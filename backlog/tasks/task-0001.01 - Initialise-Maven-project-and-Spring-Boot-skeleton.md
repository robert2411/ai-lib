---
id: TASK-0001.01
title: Initialise Maven project and Spring Boot skeleton
status: To Do
assignee: []
created_date: '2026-04-19 20:04'
updated_date: '2026-04-19 20:33'
labels:
  - scaffold
  - setup
dependencies: []
parent_task_id: TASK-1
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
pom.xml with Spring Boot 3.3.4 parent, Java 21, all required deps (web, thymeleaf, security, validation, JGit 6.x, Lucene 9.x, SnakeYAML, bucket4j). Maven wrapper. AgentLibraryApplication entry point. application.yml base config. .gitignore.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 mvnw test passes (contextLoads)
- [ ] #2 All declared dependencies resolve
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. INSTALL MAVEN (prerequisite)
   - Run: brew install maven
   - Verify: mvn --version (need Maven 3.9.x)

2. CREATE pom.xml at project root
   - Parent: org.springframework.boot:spring-boot-starter-parent:3.3.4
   - groupId: com.agentlibrary
   - artifactId: agent-library
   - version: 0.1.0-SNAPSHOT
   - packaging: jar
   - Properties:
     - java.version: 21
     - jgit.version: 6.10.0.202406032230-r
     - lucene.version: 9.11.1
     - bucket4j.version: 8.14.0
   - Dependencies (managed by BOM — no version needed):
     - org.springframework.boot:spring-boot-starter-web
     - org.springframework.boot:spring-boot-starter-thymeleaf
     - org.springframework.boot:spring-boot-starter-security
     - org.springframework.boot:spring-boot-starter-validation
     - org.springframework.boot:spring-boot-starter-test (scope: test)
     - org.springframework.security:spring-security-test (scope: test)
   - Dependencies (explicit version):
     - org.eclipse.jgit:org.eclipse.jgit:${jgit.version}
     - org.apache.lucene:lucene-core:${lucene.version}
     - org.apache.lucene:lucene-queryparser:${lucene.version}
     - org.apache.lucene:lucene-analysis-common:${lucene.version}
     - org.yaml:snakeyaml (version managed by Spring Boot BOM — 2.2)
     - com.bucket4j:bucket4j-core:${bucket4j.version}
   - Build plugins:
     - spring-boot-maven-plugin (no config needed, inherited from parent)

3. GENERATE MAVEN WRAPPER
   - Run: mvn wrapper:wrapper -Dmaven=3.9.9
   - This creates: mvnw, mvnw.cmd, .mvn/wrapper/maven-wrapper.properties
   - Verify mvnw is executable: chmod +x mvnw

4. CREATE DIRECTORY STRUCTURE
   - src/main/java/com/agentlibrary/
   - src/main/resources/
   - src/main/resources/templates/
   - src/main/resources/static/
   - src/test/java/com/agentlibrary/

5. CREATE AgentLibraryApplication.java
   - Path: src/main/java/com/agentlibrary/AgentLibraryApplication.java
   - Package: com.agentlibrary
   - Annotations: @SpringBootApplication
   - Standard main method: SpringApplication.run(AgentLibraryApplication.class, args)

6. CREATE application.yml
   - Path: src/main/resources/application.yml
   - Content:
     spring:
       application:
         name: agent-library
       security:
         user:
           name: admin
           password: admin
     server:
       port: 8080
     library:
       repo-path: ./data/library.git
       work-path: ./data/library-work
       users-file: ./data/users.yaml
   - Note: spring.security.user provides a default user so the app starts
     without needing a full UserDetailsService in this scaffold phase.
     The library.* properties are custom placeholders for future use.

7. CREATE AgentLibraryApplicationTests.java
   - Path: src/test/java/com/agentlibrary/AgentLibraryApplicationTests.java
   - Package: com.agentlibrary
   - Annotations: @SpringBootTest
   - Single test method: contextLoads() — empty body, verifies Spring context starts.
   - This test validates all dependencies resolve and auto-configuration succeeds.

8. UPDATE .gitignore (if needed)
   - The existing .gitignore already covers target/, .idea, *.iml, .mvn/wrapper/maven-wrapper.jar
   - Add if missing: /data/ (local dev data directory)
   - Ensure .mvn/wrapper/maven-wrapper.properties is NOT ignored

9. VERIFY — run the build
   - Run: ./mvnw test
   - Expected: BUILD SUCCESS, contextLoads test passes
   - If dependency resolution fails, check network/mirrors
   - If context fails to load, check for missing auto-configuration

10. FINAL CHECKS
    - Confirm all AC:
      AC#1: ./mvnw test passes (contextLoads) ✓
      AC#2: All declared dependencies resolve (verified by successful compile) ✓
    - Commit message: "feat(scaffold): initialise Maven project with Spring Boot 3.3.4 skeleton"
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Self-review complete. Plan covers all AC. No gaps or unverified assumptions.

Notes for Implementation agent:
- System has JDK 25 installed (no JAVA_HOME set). This is fine — java.version=21 in pom.xml sets --release 21 for compilation. Running on JDK 25 is forward-compatible.
- Maven is NOT installed. Step 1 (brew install maven) is required before Step 3 (wrapper generation). If brew is unavailable, alternative: install via SDKMAN (sdk install maven 3.9.9).
- Existing .gitignore already covers standard Maven/IDE patterns from Spring Initializr.
- No other tasks in the milestone need to complete before this one (no dependencies).

Analysis complete. Plan ready. No blockers.

🔍 PLAN REVIEW CONCERNS:
- Concern #1: bucket4j artifact ID is WRONG. Plan specifies `com.bucket4j:bucket4j_jdk17-core:${bucket4j.version}` but the correct artifact ID for bucket4j 8.14.0 is `com.bucket4j:bucket4j-core`. The `bucket4j_jdk17-core` naming was from an older version scheme (7.x era). This will cause Maven dependency resolution failure, violating AC#2.

Fix: Change artifactId from `bucket4j_jdk17-core` to `bucket4j-core`.

All other coordinates verified correct:
- JGit 6.10.0.202406032230-r ✓ (confirmed Maven Central)
- Lucene 9.11.1 with lucene-analysis-common ✓ (correct 9.x naming)
- Spring Boot 3.3.4 + JDK 25 ✓ (compatible)
- SnakeYAML managed by Spring BOM ✓

Verdict: Plan needs one fix before implementation (bucket4j artifactId).

Plan updated: fixed bucket4j artifact ID from bucket4j_jdk17-core to bucket4j-core per plan review feedback.

✅ PLAN APPROVED — bucket4j fix verified (now com.bucket4j:bucket4j-core), plan complete and correct.
- Steps verified: 10
- AC mapped: 2/2
- All dependency coordinates confirmed valid
- No ambiguity, no unverified assumptions remain
<!-- SECTION:NOTES:END -->
