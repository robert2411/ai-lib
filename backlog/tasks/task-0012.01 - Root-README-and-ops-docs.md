---
id: TASK-0012.01
title: Root README and ops docs
status: Done
assignee:
  - '@myself'
created_date: '2026-04-19 20:09'
updated_date: '2026-04-20 01:20'
labels:
  - docs
dependencies: []
parent_task_id: TASK-12
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
README.md: project overview, quickstart (docker compose up), configuration reference (env vars, users.yml), CLI install and usage, artifact type + harness matrix, contributing guide, API summary. CHANGELOG.md stub.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 README covers docker quickstart end-to-end
- [x] #2 Configuration table documents all required env vars
- [x] #3 Harness path matrix table is complete for all types x harnesses
- [x] #4 CLI usage examples work as documented
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
- Created README.md with all 10 required sections
- Created CHANGELOG.md stub with initial 0.1.0 release
- Harness path matrix sourced directly from InstallManifestResolver.java
- CLI examples match actual agent-lib.sh commands
- Config table covers all env vars from Dockerfile + docker-compose.yml
- Docker quickstart includes default admin credentials

All AC/DoD checked. Ready for QA.

❌ QA REJECTED: CHANGELOG.md is not in valid Keep a Changelog release-entry format.
🔍 QA REVIEW FINDINGS:
- Issue #1: [Medium] Release heading format is non-standard for Keep a Changelog. `CHANGELOG.md` uses `## 0.1.0 — Initial Release` but should follow `## [0.1.0] - YYYY-MM-DD` (and typically include `## [Unreleased]` at top). (CHANGELOG.md:8)

What passed:
- AC/DoD: All checked in task record.
- README quickstart includes `docker compose up -d`, `http://localhost:8080`, and default `admin/changeme` login. (README.md:10-17)
- Configuration table includes all required env vars: `LIBRARY_DATA_DIR`, `LIBRARY_REPO_PATH`, `LIBRARY_WORK_PATH`, `LIBRARY_USERS_FILE`, `SPRING_PROFILES_ACTIVE`. (README.md:28-32)
- Harness path matrix aligns with `HARNESS_TARGET_PATHS` mappings in `InstallManifestResolver` and correctly marks unsupported/derived cases. (README.md:170-179, InstallManifestResolver.java:25-38)
- CLI examples align with supported commands/options in `scripts/agent-lib.sh` (`search`, `list`, `info`, `install`, `pull`, `push`, `--self-test`, `--type`, `--harness`, `--version`, `--dry-run`). (README.md:115-145, scripts/agent-lib.sh:19-29, 96-103, 151-158, 260-270, 405-411)

Verdict: Fix required before approval.

Fixed QA issue #1: Updated CHANGELOG.md to use standard Keep a Changelog format:
- Added ## [Unreleased] section
- Changed heading to ## [0.1.0] - 2026-04-19 format

Ready for re-review.

✅ QA APPROVED — all tests passing, no regressions
- Re-review focus: CHANGELOG.md Keep a Changelog heading format
- Verified: `## [Unreleased]` present
- Verified: `## [0.1.0] - 2026-04-19` present
- AC/DoD: Complete (per task record)
- Security/Spelling: No new issues in reviewed file

✅ Milestone M12 complete. All tasks implemented and QA approved.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Created project root documentation files.

Changes:
- README.md: Project overview, Docker quickstart, configuration reference (5 env vars), users.yaml format with BCrypt password generation, CLI usage examples (all 6 commands), artifact types table (10 types), harness path matrix (Claude + Copilot paths from InstallManifestResolver.java), REST API summary (16 endpoints), development guide, contributing section.
- CHANGELOG.md: Keep a Changelog format stub with 0.1.0 initial release entry listing all shipped features.

Verification:
- Harness paths verified against InstallManifestResolver.java HARNESS_TARGET_PATHS map
- CLI examples verified against scripts/agent-lib.sh actual commands
- Env vars verified against Dockerfile + docker-compose.yml
<!-- SECTION:FINAL_SUMMARY:END -->
