---
id: TASK-0002.01
title: Enums and core value objects
status: In Progress
assignee: []
created_date: '2026-04-19 20:04'
updated_date: '2026-04-19 20:48'
labels:
  - domain
dependencies: []
parent_task_id: TASK-2
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
ArtifactType enum (skill, agent-claude, agent-copilot, agent-group, mcp, command, hook, claude-md, opencode, prompt). agent-group is a named set of agents (e.g. manager + implementation + analyse) that are installed and versioned together. Harness enum. Visibility enum (public, private). Filter record. VersionRef record. CommitResult record. User record. AgentGroupMember record (slug, role: manager|implementation|analyse|custom).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 All enums and records compile
- [ ] #2 Unit tests cover all enum values and record equality
- [ ] #3 ArtifactType.AGENT_GROUP enum value present
- [ ] #4 AgentGroupMember record has slug and role fields
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Create package directory: src/main/java/com/agentlibrary/model/
2. Create enum ArtifactType in com.agentlibrary.model.ArtifactType:
   - File: src/main/java/com/agentlibrary/model/ArtifactType.java
   - Values: SKILL, AGENT_CLAUDE, AGENT_COPILOT, AGENT_GROUP, MCP, COMMAND, HOOK, CLAUDE_MD, OPENCODE, PROMPT
   - Each value has a String slug field (e.g. "skill", "agent-claude", "agent-copilot", "agent-group", "mcp", "command", "hook", "claude-md", "opencode", "prompt")
   - Constructor: ArtifactType(String slug)
   - Method: public String slug() — returns the slug
   - Static method: public static ArtifactType fromSlug(String slug) — returns enum or throws IllegalArgumentException
   - The slug maps to the git repo folder name (skills/, agents-claude/, agents-copilot/, mcp/, commands/, hooks/, claude-md/, opencode/, prompts/) — store folderName as second field
   - Method: public String folderName() — returns folder path segment
3. Create enum Harness in com.agentlibrary.model.Harness:
   - File: src/main/java/com/agentlibrary/model/Harness.java
   - Values: CLAUDE, COPILOT
   - Each value has a String slug field ("claude", "copilot")
   - Constructor: Harness(String slug)
   - Method: public String slug()
   - Static method: public static Harness fromSlug(String slug)
4. Create enum Visibility in com.agentlibrary.model.Visibility:
   - File: src/main/java/com/agentlibrary/model/Visibility.java
   - Values: TEAM, PRIVATE
   - Each value has a String slug field ("team", "private")
   - Constructor: Visibility(String slug)
   - Method: public String slug()
   - Static method: public static Visibility fromSlug(String slug)
5. Create record Filter in com.agentlibrary.model.Filter:
   - File: src/main/java/com/agentlibrary/model/Filter.java
   - Fields: ArtifactType type (nullable), Harness harness (nullable), String tag (nullable), String query (nullable)
   - Compact canonical constructor (all fields nullable = no validation)
   - Use as filter criteria for artifact listing
6. Create record VersionRef in com.agentlibrary.model.VersionRef:
   - File: src/main/java/com/agentlibrary/model/VersionRef.java
   - Fields: String version, String commitId, java.time.Instant timestamp
   - Compact canonical constructor: require version non-null/non-blank, commitId non-null/non-blank
7. Create record CommitResult in com.agentlibrary.model.CommitResult:
   - File: src/main/java/com/agentlibrary/model/CommitResult.java
   - Fields: String commitId, java.time.Instant timestamp, String message
   - Compact canonical constructor: require commitId non-null/non-blank
8. Create record User in com.agentlibrary.model.User:
   - File: src/main/java/com/agentlibrary/model/User.java
   - Fields: String username, Set<String> roles
   - Compact canonical constructor: require username non-null/non-blank, roles non-null (defensive copy of set)
9. Create record AgentGroupMember in com.agentlibrary.model.AgentGroupMember:
   - File: src/main/java/com/agentlibrary/model/AgentGroupMember.java
   - Fields: String slug, String role
   - Compact canonical constructor: require slug non-null/non-blank, role non-null/non-blank
   - Document valid roles: "manager", "implementation", "analyse", "custom"
   - Static constant: Set<String> RECOGNISED_ROLES = Set.of("manager", "implementation", "analyse", "custom")
   - Method: public boolean hasRecognisedRole() — returns RECOGNISED_ROLES.contains(role)
10. Create test directory: src/test/java/com/agentlibrary/model/
11. Create ArtifactTypeTest:
    - File: src/test/java/com/agentlibrary/model/ArtifactTypeTest.java
    - Test: allValuesHaveSlugs — iterate ArtifactType.values(), assert slug() is non-null and matches expected pattern
    - Test: fromSlugRoundTrip — for each value, fromSlug(value.slug()) == value
    - Test: fromSlugInvalid — fromSlug("bogus") throws IllegalArgumentException
    - Test: agentGroupValuePresent — assertNotNull(ArtifactType.AGENT_GROUP)
    - Test: valueCount — ArtifactType.values().length == 10
12. Create HarnessTest:
    - File: src/test/java/com/agentlibrary/model/HarnessTest.java
    - Test: allValuesPresent — CLAUDE, COPILOT exist
    - Test: fromSlugRoundTrip
    - Test: fromSlugInvalid throws
13. Create VisibilityTest:
    - File: src/test/java/com/agentlibrary/model/VisibilityTest.java
    - Test: allValuesPresent — TEAM, PRIVATE exist
    - Test: fromSlugRoundTrip
14. Create FilterTest:
    - File: src/test/java/com/agentlibrary/model/FilterTest.java
    - Test: allNullsAllowed — new Filter(null, null, null, null) does not throw
    - Test: equality — two Filter records with same fields are equal
15. Create VersionRefTest:
    - File: src/test/java/com/agentlibrary/model/VersionRefTest.java
    - Test: validConstruction — new VersionRef("1.0.0", "abc123", Instant.now()) succeeds
    - Test: nullVersionThrows
    - Test: blankCommitIdThrows
    - Test: equality — same fields = equals true
16. Create CommitResultTest:
    - File: src/test/java/com/agentlibrary/model/CommitResultTest.java
    - Test: validConstruction
    - Test: nullCommitIdThrows
    - Test: equality
17. Create UserTest:
    - File: src/test/java/com/agentlibrary/model/UserTest.java
    - Test: validConstruction — new User("alice", Set.of("USER")) succeeds
    - Test: nullUsernameThrows
    - Test: blankUsernameThrows
    - Test: defensiveCopyOfRoles — mutating original set does not affect User
18. Create AgentGroupMemberTest:
    - File: src/test/java/com/agentlibrary/model/AgentGroupMemberTest.java
    - Test: validConstruction — new AgentGroupMember("my-agent", "manager") succeeds
    - Test: slugAndRoleAccessible
    - Test: nullSlugThrows
    - Test: blankRoleThrows
    - Test: hasRecognisedRole — "manager" returns true, "unknown" returns false
    - Test: equality — same slug+role = equals true
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Analysis complete. Plan ready. No blockers.

Self-review complete. Plan covers all 4 AC: (1) all enums/records defined with compilation steps, (2) tests cover all enum values and record equality, (3) AGENT_GROUP explicitly listed and tested, (4) AgentGroupMember with slug+role defined and tested. No gaps, no unverified assumptions. Dependencies: none (first task in milestone).

✅ PLAN APPROVED — plan is complete, all 4 AC covered, no ambiguity
- Steps verified: 18
- AC mapped: 4/4
- All enums have slug+fromSlug pattern, folderName on ArtifactType
- Records have proper null/blank validation
- Tests cover all enum values, round-trip fromSlug, equality, AgentGroupMember roles
- No dependencies (first task in milestone) — correct
- Minor note: AGENT_GROUP folderName not specified but implementation can resolve (e.g. "agent-groups/")
<!-- SECTION:NOTES:END -->
