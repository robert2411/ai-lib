spring:
  application:
    name: agent-library
  security:
    user:
      name: admin
      password: admin

server:
  port: 8080

library:
  repo-path: ./data/library.git
  work-path: ./data/library-work
  users-file: ./data/users.yaml
